# Disabled — ISKONNECT maintains a verified scholarship database; external scraping is not used.
# This workflow is retained only as documentation of the previous architecture.
name: Scholarship scrape and ingest (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Scraping disabled
        run: |
          echo "External scholarship scraping has been removed from ISKONNECT."
          echo "Scholarships are maintained through admin-verified imports only."
          exit 0
